<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shizuka.infrastructure.dal.mapper.DemoMapper">

    <select id="selectDrugName" resultType="java.lang.String">
        select drug_name from dd_drug where zone_id = #{zoneId} and drug_id = #{drugId}
    </select>
</mapper>